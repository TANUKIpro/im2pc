# Docker Compose Environment Variables
# Copy this file to .env and adjust the values for your environment.
#
# HOST_DATA_DIR: Absolute path to the data directory on the host machine.
# This is required for the inference server (running on host) to access video files.
# Example for macOS/Linux:
#   HOST_DATA_DIR=/Users/yourname/workspace/pi3x/data
# Example for Windows:
#   HOST_DATA_DIR=C:/Users/yourname/workspace/pi3x/data

HOST_DATA_DIR=/path/to/your/pi3x/data
